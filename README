This folder is an eclipse workspace
->Codecaster is a project made with the Eclipse Plugin Developer Environment

->Codecaster Drop-In JAR is where i have published the plugin.
    Drop the jar into your eclipse dropin/plugin folder thingy
    Restart eclipse
    Hit the new TOGGLE command that appears beside the help menu
    The IP adress and port are built in because i dont know how to make interfaces in eclipse
    127.0.0.1:80 is where it broadcasts to

->Python server
    There should be a bat file in here that starts up a python folder. this uses the
    latest build of python
    this server uses the address 0.0.0.0:80

->Only checked with Google Chrome browser
    Navagate to 127.0.0.1/viewer.htm
